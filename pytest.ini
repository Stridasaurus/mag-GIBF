[pytest]
# Restrict collection to this repo's own suite. Without this, a bare
# `pytest -q` at the root recurses into the secsy submodule's test_scripts/,
# which import lompe (not a dependency of this project) and abort collection.
testpaths = tests
